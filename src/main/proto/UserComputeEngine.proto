package usercompute;

message FindFactorsRequest {
    repeated int32 nums = 1;
}

message FindFactorsResponse {
    repeated string factors = 1;
}

service UserComputeEngineService {
    rpc FindFactors(FindFactorsRequest) returns (FindFactorsResponse);
}
